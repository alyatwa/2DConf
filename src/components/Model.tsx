import React, { Suspense, useState } from "react";
 
import { ITemp } from "../Types/interfaces";

interface TypeModel {
}

 

const Model = (props: ITemp) => {
	 

	return (
		<>
		</>
	);
};

export default Model;
